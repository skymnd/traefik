http:
  routers:
    immich-router:
      rule: Host(`immich.{{env "HOMELAB_DOMAIN"}}`)
      entryPoints:
        - websecure
      service: immich
      tls:
        certResolver: le

  services:
    immich:
      loadBalancer:
        servers:
          - url: http://{{env "IMMICH_IP"}}:2283
