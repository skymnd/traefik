http:
  routers:
    truenas-router:
      rule: Host(`truenas.{{env "HOMELAB_DOMAIN"}}`)
      entryPoints:
        - websecure
      service: truenas
      tls:
        certResolver: le

  services:
    truenas:
      loadBalancer:
        servers:
          - url: http://{{env "TRUENAS_IP"}}
